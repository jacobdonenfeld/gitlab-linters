-   id: gitlab-ci-shellcheck
    name: Check gitlab scripts
    entry: gitlab-ci-shellcheck
    pass_filenames: false
    language: python
    types: [yaml]
    files: '(\.yml|\.yaml)$'
    description: Validate gitlab ci yaml scripts via shellcheck and AST parsing
#-   id: gitlab-ci-linter
#    name: Lint gitlab .yml via api
#    entry: gitlab-ci-lint
#    language: python
#    files: '(\.yml|\.yaml)$'
#    types: [yaml]
#    description: API call to gitlab's linter to validate .yml config

